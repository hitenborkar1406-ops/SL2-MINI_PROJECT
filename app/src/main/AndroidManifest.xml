<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Gallery access for older devices (API ≤ 32); not required on API 33+ with PickVisualMedia -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- System notification permission required on Android 13+ (API 33+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MINI_PROJECT">

        <!-- Profile edit screen -->
        <activity
            android:name=".ProfileActivity"
            android:exported="false" />

        <!-- First-launch user setup screen -->
        <activity
            android:name=".UserSetupActivity"
            android:exported="false" />

        <!-- Add lost/found item screen -->
        <activity
            android:name=".AddItemActivity"
            android:exported="false" />

        <!-- Item detail & delete screen -->
        <activity
            android:name=".DetailActivity"
            android:exported="false" />

        <!-- Home screen (launcher entry point) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>